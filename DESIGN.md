# CastTimer 视觉设计

## 设计意图

这是一个在水边快速阅读、单手操作的计时器。界面像平静水面：信息少、留白多；唯一的暖色浮漂告诉钓者当下应注意什么。

## 视觉系统

- **深水背景**：`#031B29`，让 AMOLED 屏幕舒适、在阳光下仍可保持高对比。
- **水面与雾色文字**：`#0C5264`、`#A8E4DF`、`#D8F4F0`，用于分层和主读数。
- **琥珀浮漂**：`#FFB34D`，仅标识主操作、正在计时的浮漂和完成状态。
- **警示珊瑚色**：`#FF7262`，只在超时、结束确认等需要谨慎的场景出现。

## 交互层级

每一页只回答三个问题：现在是什么状态、最重要的读数是什么、按“选择”会做什么。状态通过浮漂、水纹、鱼钩和抛竿线条区分；这些图形是简化符号，不依赖装饰图片，在小圆屏上依然清晰。

## 核心流程与屏幕

界面遵循一条可被腕上震动打断、也能随时手动接管的闭环：

```text
准备识别 → 等待第一次抛竿 → 本轮倒计时 → 到点换饵 → 下一次抛竿
                         ↑                         │
                         └──────── 自动进入下一轮 ──┘
                                             ↓
                                        收竿总结
```

- **准备识别**：只保留一个可调的换饵间隔和一个开始动作，说明“抛竿后自动重置”。
- **等待抛竿**：明确展示自动识别是否就绪；识别不可用时，将手动补记作为同等清晰的兜底路径。
- **倒计时**：用中央圆形计时盘承载唯一的大读数；顶部显示识别模式与会话时长，底部展示当前轮次、总抛竿数和 10 秒撤销窗口。
- **到点换饵**：珊瑚色只在此状态出现，文案直接给出下一步；再次抛竿会清除超时并开启新一轮。
- **收竿总结**：先给出完成感和作钓时长，再给出抛竿总数、自动识别数与手动补记数，形成一次作钓的清晰句点。

收竿页是一次完整作钓的句点：以钓鱼勋章、按抛竿次数授予的称号和轻快的完成震动表达成就，而不是把它处理成冷冰冰的统计列表。

## 布局与可读性

界面按屏幕高度的比例定位，覆盖 fēnix 7、Forerunner 255 和 Venu 3 的不同圆屏尺寸。顶部仅保留小型英文状态标签，中央留给主读数，底部固定一条操作栏。所有文字保持高对比，不能只依赖颜色表达状态。
