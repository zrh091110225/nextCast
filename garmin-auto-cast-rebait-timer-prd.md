# Garmin 自动抛竿识别与换饵提醒应用 PRD

> 文档类型：产品需求文档（PRD）+ 开发评估输入  
> 工作名称：CastTimer / 抛竿计时  
> 平台：Garmin Connect IQ 独立手表应用（Watch App）  
> 版本：v0.9  
> 状态：待产品、算法、Connect IQ 开发联合评审  
> 更新日期：2026-07-31  
> 首发语言：简体中文；英文作为商店发布前必备语言

---

## 1. 一句话定义

用户开始垂钓后，Garmin 手表持续识别抛竿动作；每次确认抛竿后自动开始或重置换饵倒计时；时间到后通过腕上震动提醒换饵；用户再次抛竿后自动进入下一轮计时，从而形成无需反复操作手表或手机的闭环。

```text
识别抛竿
   ↓
自动开始/重置倒计时
   ↓
到点震动提醒换饵
   ↓
用户换饵并再次抛竿
   ↓
自动进入下一轮
```

## 2. 背景与问题

在部分静态垂钓场景中，饵料入水后需要按相对稳定的间隔重新挂饵、补饵或抛竿。例如钓小鱼时可能每 2～3 分钟重新抛竿，钓鲤鱼等目标鱼时可能每 5～6 分钟重新抛竿。

当前用户主要依赖：

- 凭感觉估算时间；
- 不断查看手机或手表；
- 每次抛竿后手动启动计时器；
- 使用普通循环计时器，但实际换饵、遛鱼或处理异常后节奏会错位。

这些方案的问题是，用户需要主动记住“开始计时”或“重置计时”。一旦忘记，提醒节奏便与实际抛竿节奏脱节。

本产品解决的不是“缺少计时器”，而是：

> 用户需要计时，但不愿意、也经常无法在每次抛竿后手动操作计时器。

## 3. 产品目标

### 3.1 MVP 目标

1. 在明确支持的钓法、佩戴手和设备范围内，可靠识别真实抛竿动作。
2. 每次抛竿后，在 2 秒内自动开始或重置倒计时。
3. 倒计时到点时，以清晰、可区分的震动提醒用户换饵。
4. 下一次抛竿后自动清除超时状态并进入新一轮倒计时。
5. 全程脱离手机工作，并保留手动触发和撤销作为兜底。
6. 记录本次垂钓时长、抛竿次数和自动识别质量，支持后续算法改进。

### 3.2 业务目标

- 验证“自动识别抛竿 + 自动计时”是否显著优于普通手动计时器。
- 验证用户是否愿意在一次 2～6 小时的垂钓中持续运行应用。
- 验证不同用户、设备、持竿方式下，通用抛竿模型是否可达到可用精度。
- 为后续抛竿统计、中鱼识别、钓获记录等功能建立传感器与会话框架。

### 3.3 非目标

以下内容不进入 MVP：

- 自动识别中鱼、咬口或提竿；
- 自动判断饵料是否已经脱落；
- 自动推荐最佳换饵间隔；
- GPS 钓点、天气、气压、潮汐和鱼情分析；
- 多鱼竿并行计时；
- 钓获种类、重量和照片记录；
- 社区、排行榜和账号系统；
- 手机端配套 App；
- 语音播报或任意文本转语音；
- 飞钓、路亚连续抛投训练、海钓铁板等高频连续抛投场景；
- 与 Garmin 原生钓鱼活动同时运行。

## 4. 关键产品假设与 Go/No-Go 条件

### 4.1 最关键假设

用户佩戴手表的手腕，在抛竿动作中能够产生足够稳定且可区分的运动信号。

该假设不能仅靠模拟器验证，必须采集真实水边抛竿数据。

### 4.2 佩戴手风险

很多用户将手表戴在非惯用手，而持竿、发力和控线方式因人而异：

- 双手抛竿时，两侧手腕可能都有明显信号；
- 单手抛竿时，非持竿手的信号可能很弱；
- 纺车轮、鼓轮和不同持竿姿势的轴向信号不同。

因此 MVP 的默认支持范围为：

> 手表佩戴在主要持竿/发力手，或使用双手抛竿且该手腕参与明显发力。

首次开始会话前，用户必须选择：

- 手表佩戴：左手 / 右手；
- 主要持竿手：左手 / 右手；
- 抛竿方式：双手 / 单手。

如果手表不在主要发力手，应用需要提示“自动识别准确率可能下降”，但允许用户继续测试。

### 4.3 Phase 0 继续开发门槛

真实场景数据达到以下指标后，才进入完整 MVP 开发：

| 指标 | 继续开发门槛 |
|---|---:|
| 支持场景下抛竿识别 Precision | ≥ 95% |
| 支持场景下抛竿识别 Recall | ≥ 90% |
| 非抛竿动作误触发 | ≤ 0.5 次/小时 |
| 抛竿到计时开始的中位延迟 | ≤ 1.5 秒 |
| 6 小时连续运行稳定性 | 无崩溃、无传感器停止 |
| 候选首发机型的有效加速度采样率 | 实测不低于 25Hz，或以实测较低采样率重新通过上述识别门槛 |

若“手表戴在非持竿手”的 Recall 低于 75%，首版应明确标记该佩戴方式不受支持，而不是降低整体判定阈值、引入大量误触发。

## 5. 目标用户与首发场景

### 5.1 核心用户

- 使用 Garmin 手表的休闲或高频垂钓者；
- 采用定点、守钓或固定节奏补饵的钓法；
- 每次垂钓持续 1 小时以上；
- 已有凭经验看时间、手机计时或循环闹钟的习惯；
- 希望提高单位时间内有效抛竿次数。

### 5.2 MVP 支持场景

- 岸钓或固定钓位；
- 纺车轮或鼓轮；
- 过头抛、侧抛；
- 每两次抛竿通常间隔 30 秒以上；
- 一次只使用一根主要鱼竿；
- 手表应用在整个会话中保持为当前活动应用；这是到点震动的产品保证前提。

### 5.3 MVP 不保证支持

- 飞钓的连续挥杆和假抛；
- 路亚搜索式高频连续抛投；
- 船体剧烈晃动环境；
- 用打窝勺、投饵器频繁投饵；
- 一人同时操作多根鱼竿；
- 手表完全不参与发力的单手抛竿；
- 用户退出应用或切换到其他活动后仍按时震动；应用恢复前台后仅补显示超时状态，不承诺补发等时的提醒。

## 6. 用户故事

### 6.1 核心故事

1. 作为钓鱼人，我希望选择一个换饵间隔后开始垂钓，而不需要复杂设置。
2. 作为钓鱼人，我希望手表识别我的第一次抛竿并自动开始计时。
3. 作为钓鱼人，我希望每次重新抛竿都自动重置计时，无需触碰手表。
4. 作为钓鱼人，我希望到点后通过明确震动知道应该换饵，即使我没有看表。
5. 作为钓鱼人，我希望误识别时可以快速撤销，避免计时被错误重置。
6. 作为钓鱼人，我希望漏识别时可以一键手动记录抛竿，继续正常计时。
7. 作为钓鱼人，我希望暂停处理鱼、换线或休息，恢复后继续使用。
8. 作为钓鱼人，我希望结束后看到垂钓时长和抛竿次数。

## 7. 产品范围与优先级

### 7.1 P0：必须具备

- 选择/调整换饵间隔；
- 开始、暂停、恢复、结束会话；
- 高频加速度采集；
- 自动抛竿识别；
- 抛竿后自动开始/重置倒计时；
- 到点震动；
- 超时状态；
- 手动记录抛竿；
- 撤销最近一次抛竿；
- 抛竿次数统计；
- 会话状态持久化和异常恢复；
- 不支持设备/传感器错误提示；
- 按键设备与触屏设备的基本适配。

### 7.2 P1：Beta 建议具备

- 个性化快速校准；
- 抛竿识别灵敏度设置；
- 可选提示音；
- 最近 20 次会话摘要；
- 误识别/漏识别标记；
- 诊断模式与原始传感器日志导出；
- FIT 开发者字段记录抛竿总数；
- 英文界面。

### 7.3 P2：后续版本

- 自动识别提竿/中鱼；
- 钓获记录；
- GPS 钓点与环境数据；
- 多种抛竿模型自动选择；
- 手机端模型更新和数据同步；
- 多鱼竿状态管理；
- 用户自定义震动节奏；
- 模型持续个性化。

## 8. 核心交互流程

### 8.1 首次使用

1. 打开应用。
2. 阅读一页核心说明：应用必须在垂钓期间保持运行；建议戴在主要发力手。
3. 选择手表佩戴手、持竿手和主要抛竿方式。
4. 选择换饵间隔。
5. 选择“开始垂钓”。
6. 应用启动 Fishing 活动记录并进入“等待第一次抛竿”。

### 8.2 日常启动

1. 打开应用。
2. 默认选中上次使用的换饵间隔。
3. 用户按一次确认键开始。
4. 进入“等待抛竿”。

目标：老用户从打开应用到进入识别状态不超过 3 次按键。

### 8.3 正常闭环

1. 用户抛竿。
2. 应用在 2 秒内识别并短震一次。
3. 屏幕显示倒计时和本次第 N 次抛竿。
4. 倒计时归零时，应用执行换饵震动。
5. 用户换饵并再次抛竿。
6. 应用识别新抛竿，停止超时提醒并重置倒计时。

### 8.4 提前重新抛竿

如果用户在倒计时结束前重新抛竿：

- 立即将本次识别计为新抛竿；
- 将计时终点重置为“新抛竿时间 + 当前间隔”；
- 短震确认；
- 不保留上一轮剩余时间。

### 8.5 漏识别兜底

用户完成抛竿，但应用没有自动识别时：

- 按“确认/开始”键执行“手动记录抛竿”；
- 立即开始或重置倒计时；
- 该事件标记为 `manual_cast`；
- 计入总抛竿次数。

### 8.6 误识别撤销

应用识别抛竿后 10 秒内：

- 屏幕显示“撤销”提示；
- 用户按返回键撤销；
- 恢复识别前的倒计时终点、抛竿次数和状态；
- 该事件标记为 `auto_cast_undone`；
- 撤销后进入 3 秒冷却，避免同一动作再次触发。

10 秒后仍允许通过菜单撤销最近一次事件，但不作为主路径。

## 9. 状态机

### 9.1 状态定义

| 状态 | 含义 |
|---|---|
| `SETUP` | 选择间隔和会话参数 |
| `ARMED` | 会话已开始，等待第一次抛竿 |
| `COUNTING` | 已识别抛竿，倒计时进行中 |
| `DUE` | 倒计时已到，等待用户换饵并再次抛竿 |
| `PAUSED` | 会话暂停，不识别动作、不触发到点提醒 |
| `ENDING` | 结束确认和摘要生成 |

### 9.2 状态转换

| 当前状态 | 事件 | 下一状态 | 行为 |
|---|---|---|---|
| `SETUP` | 开始会话 | `ARMED` | 启动活动记录和传感器 |
| `ARMED` | 自动/手动抛竿 | `COUNTING` | 计数 +1，创建计时终点 |
| `COUNTING` | 新抛竿 | `COUNTING` | 计数 +1，重置计时终点 |
| `COUNTING` | 到达终点 | `DUE` | 执行到点震动 |
| `DUE` | 新抛竿 | `COUNTING` | 停止超时提醒，开始新周期 |
| 任意活动状态 | 暂停 | `PAUSED` | 停止识别和提醒，保存原状态 |
| `PAUSED` | 恢复 | 原活动状态 | 按暂停策略恢复 |
| 任意活动状态 | 结束 | `ENDING` | 停止传感器，保存/结束活动 |
| 自动抛竿后的 10 秒 | 撤销 | 识别前状态 | 回滚事件和计时终点 |

### 9.3 暂停策略

MVP 采用“冻结剩余时间”：

- 暂停时记录剩余秒数；
- 暂停期间不减少剩余时间；
- 恢复后以该剩余秒数继续；
- 用户也可以直接选择“等待下一次抛竿”，清除当前倒计时并返回 `ARMED`。

## 10. 功能需求

### FR-001 换饵间隔选择

- 应用首次打开、或上一会话结束后，必须先显示换饵间隔设置页；用户以 30 秒为步长设置 30 秒～10 分钟的间隔。
- 应用必须持久化最近一次确认使用的换饵间隔，并在下次进入设置页时默认选中该值。
- 默认值为用户上次使用值；首次使用默认 3 分钟。
- 提供快捷预设：
  - 快节奏：2 分钟；
  - 标准：3 分钟；
  - 慢节奏：5 分钟；
  - 自定义。
- 可以显示“鲫鱼/小鱼”“鲤鱼/大鱼”等建议标签，但底层只保存时间值。
- 预设不得表述为科学保证，用户可随时调整。

### FR-002 会话开始

- 用户确认开始后，应用必须：
  1. 检查高频加速度传感器能力；
  2. 检查震动能力；
  3. 按 WP0 已验证的安全流程检查活动记录冲突；
  4. 创建并启动应用自有的 `SPORT_FISHING` 活动记录；
  5. 注册传感器监听；
  6. 进入 `ARMED`。
- Connect IQ 的 `createSession()` 在已有未关闭 Session 时可能返回既有对象；因此，在 WP0 确认可可靠识别冲突和所有权前，应用必须把疑似已有活动视为启动失败。不得对非本应用创建的 Session 调用 `start()`、`stop()`、`save()` 或 `discard()`，也不得复用或接管它。
- 如果震动或活动记录不可用，则不能进入本次会话。
- 如果仅高频动作传感器不可用，应用可以提供“手动计时模式”，但必须持续显示“自动识别已关闭”，所有抛竿均由用户按键记录。
- 任一步骤失败时不得假装自动识别已经开始，必须显示可理解的错误和可用的后续操作。

### FR-003 自动抛竿识别

- 仅在 `ARMED`、`COUNTING`、`DUE` 状态下识别。
- 检测到符合条件的动作后，在 2 秒内生成 `auto_cast` 事件。
- 每个事件包含：时间戳、置信度、模型版本、佩戴手、设备型号和触发来源。
- 识别完成后进入至少 5 秒不应期，避免一次抛竿被重复计数。
- 不应期内的手动抛竿仍可记录，但需要二次确认。

### FR-004 自动开始和重置倒计时

- 第一次抛竿创建 `deadline = castTime + intervalSec`。
- 后续每次抛竿覆盖旧 `deadline`。
- 显示剩余时间时必须由绝对终点重新计算，不以 UI 每秒累减值作为真实时间源。
- 调整间隔时，默认从最近一次抛竿重新计算；修改前需确认。

### FR-005 抛竿确认反馈

- 每次自动或手动抛竿成功记录后，执行一次 100～150ms 短震。
- 短震必须与“到点换饵”震动明显不同，并以段数、时长和间隔区分；不得仅依赖震动强度，因为部分 Forerunner 设备不支持可感知的强度模式。
- 用户可以关闭抛竿确认短震，但不能关闭到点震动后仍声称提醒功能正常。

### FR-006 到点提醒

- 当设备时间达到 `deadline` 时：
  - 进入 `DUE`；
  - 执行三段明显震动；
  - 屏幕显示“该换饵了”；
  - 记录 `rebait_due` 事件。
- 默认重复策略：
  - 到点：三段中等长度震动；
  - 超时 60 秒：两段长震；
  - 超时 120 秒：两段长震；
  - 此后停止重复，屏幕保持超时状态。
- 用户可在设置中关闭重复提醒，但首个到点提醒必须保留。
- 新抛竿发生后立即清除 `DUE` 并进入新周期。
- 应用 inactive 时不承诺执行震动；此时仍保存 `DUE` 状态，恢复 active 后重新计算状态并显示已超时，不把补发震动当作准时提醒。

### FR-007 可选提示音

- 若设备支持 `Attention.playTone()`，用户可以开启系统预定义提示音。
- 运行时必须使用能力检查，不能假设所有 Garmin 设备都有音调发生器。
- MVP 不提供录音、语音文件或文本转语音。
- 震动是主提醒通道，声音仅为增强通道。

### FR-008 手动记录抛竿

- 活动主界面始终提供单键手动记录入口。
- 手动记录的计时和自动记录完全一致。
- 手动事件单独统计，用于判断自动识别质量。

### FR-009 撤销

- 支持撤销最近一次自动或手动抛竿。
- 撤销必须恢复：
  - 抛竿次数；
  - 上一次计时终点；
  - 上一次状态；
  - 到点重复提醒计划。
- 连续撤销仅要求支持最近 1 次；历史多步撤销不进入 MVP。

### FR-010 暂停与恢复

- 暂停后停止传感器识别和到点提醒。
- FIT 活动记录可以暂停或继续，具体实现由技术评审决定，但用户界面只显示一个统一暂停状态。
- 恢复时让用户选择：
  - 继续剩余时间；
  - 等待下一次抛竿。

### FR-011 结束会话

- 结束操作必须二次确认，避免误触。
- 结束时：
  - 注销传感器监听；
  - 停止所有 Timer；
  - 停止并保存 FIT 活动；
  - 保存会话摘要；
  - 展示结果页面。
- 摘要至少包含：垂钓时长、总抛竿次数、自动识别次数、手动次数、撤销次数、平均抛竿间隔。

### FR-012 异常恢复

- 当前会话关键状态应在以下时机持久化：
  - 会话开始；
  - 每次抛竿；
  - 暂停/恢复；
  - 到点；
  - 应用进入非活动状态；
  - 会话结束。
- 应用重新打开时，如果发现未正常结束的会话：
  - 显示“恢复上次会话”；
  - 根据保存的绝对 `deadline` 恢复倒计时或显示已经超时；
  - 明确提示退出期间的震动不保证已经触发。

## 11. 主界面信息架构

### 11.1 设置页

必须显示：

- 当前换饵间隔；
- 快捷预设；
- 开始垂钓；
- 佩戴/持竿设置入口；
- 最近一次会话摘要入口。

### 11.2 等待第一次抛竿

```text
等待抛竿

自动识别中
间隔 03:00

[确认键：手动记录]
```

### 11.3 倒计时页

```text
    02:37
距换饵

第 12 次抛竿
间隔 03:00

[确认键：手动重置]
```

设计要求：

- 剩余时间是最大视觉元素；
- 低分辨率黑白屏仍能读取；
- 不依赖颜色表达关键状态；
- 阳光下可读；
- 触屏和五键机型均可操作。

### 11.4 到点页

```text
该换饵了

已超时 00:42
第 12 次抛竿

再次抛竿后自动重置
```

### 11.5 会话摘要

```text
垂钓 03:26:18
抛竿 46 次
自动识别 43
手动补记 3
撤销误识别 1
平均间隔 04:21
```

## 12. 按键与输入映射

必须优先使用 Garmin `BehaviorDelegate` 的语义行为，避免绑定单一机型的物理键。

建议行为：

| 行为 | 活动主界面 |
|---|---|
| Select / Tap | 手动记录抛竿 |
| Back | 10 秒内撤销；否则返回前需确认结束 |
| Menu | 打开暂停、调整间隔、结束会话菜单 |
| Up/Down 或 Swipe | 切换倒计时页、统计页、状态页 |

最终映射须在按钮型和触屏型首发设备上分别验证。

## 13. 抛竿识别技术需求

### 13.1 传感器输入

MVP 基线：

- 三轴加速度计；
- 25Hz 采样率作为首轮实现起点；
- 每次回调请求 1 秒数据；
- 保留最近 3～4 秒环形缓冲；
- 陀螺仪在设备支持时作为增强特征，不作为首版设备准入硬依赖。

Garmin `Sensor.registerSensorDataListener()` 支持高频加速度与陀螺仪数据；单次请求周期最长 4 秒，同类型只允许注册一个请求。启动时必须通过 `Sensor.getMaxSampleRateForSensorType(:accelerometer)`（或兼容 API）读取能力，且在每个候选设备上用回调样本数和时间戳实测有效采样率；不得将请求的 25Hz 视为已获得的 25Hz。

### 13.2 推荐识别管线

```text
高频传感器流
   ↓
去重力/滤波与坐标归一化
   ↓
运动能量门控，发现候选动作
   ↓
截取候选前后短窗口
   ↓
规则或轻量分类器判断
   ↓
状态约束 + 不应期去重
   ↓
生成抛竿事件
```

识别必须采用因果决策：候选动作触发后只能等待满足 P95 ≤ 2 秒所允许的短后验窗口。不得为了取得完整的 1～2 秒后续动作而牺牲 FR-003 的事件时延；较完整窗口只可用于离线训练和回放分析。

### 13.3 候选特征

开发评估至少考虑：

- 三轴和合加速度峰值；
- 加速度变化率（jerk）；
- 峰值前后的方向变化顺序；
- 动作持续时间；
- 动作前静止程度；
- 动作后减速特征；
- 轴间能量比例；
- 陀螺仪峰值与角速度方向；
- 左右手坐标镜像归一化；
- 与手表朝向无关的合加速度、轴间比例和方向变化特征；左右镜像不能单独解决佩戴松紧与持竿姿势差异；
- 单手/双手模式；
- 不同设备采样抖动和量程差异。

### 13.4 算法策略

首版推荐采用两阶段方案：

1. 低成本阈值/滤波器找候选动作；
2. 对候选窗口执行轻量规则树、线性模型或小型定点参数模型。

原因：Connect IQ 不提供 Core ML、TensorFlow Lite 等通用端侧推理框架，Monkey C 的内存和计算预算也因设备而异。模型应在离线环境训练，最终导出为少量特征权重、阈值或紧凑决策树。

Garmin SDK 自带 `PitchCounter` 示例已经演示了：

- 25Hz 加速度采样；
- FIR 滤波；
- 投掷候选阈值；
- 动作间隔去重；
- SensorLogging 与 ActivityRecording 联用。

该示例只能作为工程骨架。钓鱼抛竿的动作、持竿方向和负样本与棒球投掷不同，必须重新采集和训练。

`SensorLogging` 仅用于 Beta 诊断和回放数据获取，其记录速率由平台决定，不能假定等同于监听器请求速率。WP0 必须通过日志的 `sampleCount`、`samplePeriod` 及导出 FIT 文件确认各设备的实际数据完整性；若无法取得足够分辨率的回放数据，应另行设计经用户同意的候选窗口导出方案，且不得写入正式版长期存储。

### 13.5 必须覆盖的负样本

- 收线；
- 快速扬竿或刺鱼；
- 抄鱼、提鱼；
- 挂饵、摘钩、剪线；
- 甩手上的水；
- 喝水、吃东西；
- 走路、跑动、搬椅子；
- 手动投撒饵料；
- 调整鱼竿支架；
- 将鱼竿放下或拿起；
- 船体/平台晃动；
- 与同伴挥手或拿手机。

### 13.6 校准方案

Beta 提供“快速校准”，不强制普通用户首次完成：

1. 用户进入校准模式；
2. 正常完成 5 次抛竿；
3. 每次抛竿后 3 秒内按确认键；
4. 应用将按键前的传感器窗口标记为正样本；
5. 根据峰值、轴向和持续时间调整个体阈值。

校准必须有“恢复默认模型”入口。首版不在手表上训练复杂模型。

## 14. Garmin Connect IQ 技术方案

### 14.1 应用类型

必须使用独立 `watch-app` / Device App，而不是：

- Watch Face：无法持续使用所需传感器和计时能力；
- Widget/Glance：从 Glance 启动存在超时行为，不适合数小时会话；
- Data Field：官方文档明确指出在 data field 中调用高频传感器注册会导致崩溃。

用户应从活动/应用菜单启动。Garmin 文档说明，从活动菜单启动的应用不会像 Glance 启动那样自动超时。

### 14.2 活动记录

应用开始会话时创建 `ActivityRecording.Session`：

- `sport = Activity.SPORT_FISHING`；
- 应用名建议为 `CastTimer`；
- 会话开始时 `session.start()`；
- 暂停策略待技术 Spike 验证；
- 结束时 `session.stop()` + `session.save()`。

使用活动记录的原因：

- 活动状态下高频传感器访问更稳定；
- 新生命周期设备进入 inactive 时，若活动仍在记录，高频传感器访问仍可获得；
- 可以在 Garmin Connect 中保留本次垂钓活动。

重要限制：

- Garmin 同一时间只能存在一个活动记录 Session；
- 用户不能同时运行 Garmin 原生“钓鱼”活动和本应用；冲突检测和安全失败路径是 WP0 发布阻断项；
- 本应用必须承担该次钓鱼活动的入口和基础记录；
- 若用户切换到其他 App，`Attention` 在 inactive 状态不可用。即使活动记录使高频传感器仍可访问，到点震动也不能保证，因此产品必须提示用户保持本应用为当前活动；恢复 active 时必须由 `CountdownEngine` 按绝对 `deadline` 对账并更新为 `COUNTING` 或 `DUE`。

### 14.3 建议权限

正式版：

```xml
<iq:uses-permission id="Fit"/>
<iq:uses-permission id="Sensor"/>
```

Beta/诊断版额外使用：

```xml
<iq:uses-permission id="SensorLogging"/>
```

MVP 不申请：

- `Positioning`；
- `Communications`；
- `Background`；
- `UserProfile`。

如果后续加入 GPS、数据上传或后台服务，再单独评审权限和隐私文案。

### 14.4 主要 API

| 能力 | Connect IQ API | 用途 |
|---|---|---|
| 高频动作数据 | `Toybox.Sensor.registerSensorDataListener()` | 抛竿识别 |
| 停止动作数据 | `Sensor.unregisterSensorDataListener()` | 暂停/结束释放资源 |
| 活动记录 | `Toybox.ActivityRecording` | 保持活动会话并生成 FIT |
| 震动 | `Toybox.Attention.vibrate()` | 抛竿确认和换饵提醒 |
| 可选提示音 | `Toybox.Attention.playTone()` | 支持设备上的辅助提醒 |
| UI 刷新 | `WatchUi.requestUpdate()` | 更新倒计时和状态 |
| UI 定时 | `Toybox.Timer.Timer` | 1 秒界面刷新和短期事件 |
| 本地持久化 | `Application.Storage` | 当前状态、设置和摘要 |
| 设备能力 | `System.getDeviceSettings()` / `Toybox has` | 能力判断和降级 |

### 14.5 计时实现要求

`Toybox.Timer.Timer` 只用于触发 UI 刷新和短期回调，不作为真实倒计时的数据源。

真实剩余时间必须按以下公式计算：

```text
remainingSec = deadlineEpochSec - currentEpochSec
```

这样可以避免：

- UI 回调延迟造成累计误差；
- 重复 Timer 漏执行；
- 应用 inactive/恢复后计时错误；
- 屏幕刷新频率变化导致倒计时漂移。

### 14.6 建议模块划分

```text
CastTimerApp
├── AppLifecycleController
├── SessionController
├── CastDetector
│   ├── SensorBuffer
│   ├── SignalFilter
│   ├── CandidateGate
│   └── CastClassifier
├── CountdownEngine
├── ReminderManager
├── ActivityRecorder
├── SessionRepository
├── SettingsRepository
├── CapabilityChecker
├── Views
│   ├── SetupView
│   ├── ActiveSessionView
│   ├── DueView
│   └── SummaryView
└── InputDelegate
```

模块边界要求：

- `CastDetector` 不直接操作 UI 或计时器，只产生带置信度的事件；
- `CountdownEngine` 只维护状态和绝对终点；
- `ReminderManager` 统一管理所有震动/声音，避免重复触发；
- `SessionController` 是状态机唯一写入口；
- 设备差异通过能力层和资源覆盖处理，不在业务逻辑中散落型号判断。

### 14.7 可复用的官方实现

开发不应从空白工程重新摸索以下底层能力：

| Garmin SDK 示例 | 可复用内容 | 不可直接复用内容 |
|---|---|---|
| `samples/PitchCounter` | 25Hz 加速度监听、FIR 滤波、动作去重、SensorLogging 与活动记录组合 | 棒球投球阈值和判定轴 |
| `samples/AccelMag` | 传感器启用、数据回调、模拟器数据读取 | 抛竿分类逻辑 |
| `samples/RecordSample` | ActivityRecording Session 的创建、开始、停止和保存 | 产品状态机和异常恢复 |
| `samples/Sensor` | 设备传感器能力与空值处理方式 | 高频动作窗口管理 |

开发环境应先升级并锁定到当时最新稳定 Connect IQ SDK。本文编写时官网公开版本为 9.2.0，本机参考示例来自 9.1.0；升级后必须重新编译示例并确认 API 行为没有变化。

## 15. 设备支持策略

### 15.1 首轮开发测试机型

建议至少覆盖四种产品形态：

1. fēnix 7 或 7 Pro：户外主力、按键、MIP；
2. Forerunner 255/265：主流轻量运动表；
3. Venu 3：AMOLED、触屏。

Instinct 2 仅支持 Connect IQ API 3.4；本项目首发最低 API 版本为 4.2，因此 Instinct 2 不纳入首发白名单。后续若要支持，应单独评估 API 降级、内存约束、特殊屏幕布局与传感器能力，不能视为当前版本的兼容范围。

### 15.2 首发白名单原则

设备同时满足以下条件才进入商店支持列表：

- 支持 `SensorData.accelerometerData`；
- 支持 25Hz 或实测足够的高频加速度；
- 支持 `ActivityRecording`；
- 支持震动；
- 6 小时压力测试通过；
- 内存和 UI 适配通过；
- 该设备上的识别精度不显著低于总体指标。

陀螺仪不是准入硬条件；可通过能力检测启用增强模型。

不建议在首版 manifest 中一次性声明全部兼容设备，应在真实设备测试后逐步扩大白名单。

## 16. 数据模型

### 16.1 当前会话

```text
CurrentSession
- schemaVersion
- sessionId
- startedAtEpoch
- state
- previousState
- intervalSec
- deadlineEpoch
- pausedRemainingSec
- castCount
- autoCastCount
- manualCastCount
- undoneCastCount
- dueReminderCount
- lastCastEvent
- wristSide
- castingHand
- castMode
- detectorModelVersion
- deviceModel
```

### 16.2 抛竿事件

```text
CastEvent
- eventId
- occurredAtEpoch
- source: auto | manual
- confidence
- intervalSec
- previousDeadlineEpoch
- modelVersion
- undone
```

### 16.3 会话摘要

```text
SessionSummary
- sessionId
- startedAtEpoch
- endedAtEpoch
- durationSec
- intervalSec
- castCount
- autoCastCount
- manualCastCount
- undoneCastCount
- dueReminderCount
- averageCastIntervalSec
- modelVersion
- deviceModel
```

### 16.4 存储策略

- 当前会话使用小型 Dictionary 持久化；
- 仅保存最近 20 个会话摘要；
- 正式版不在 `Application.Storage` 保存连续原始传感器数据；
- 单个 Storage value 不能接近平台 32KB 上限；
- Beta 原始数据优先通过 `SensorLogging` 写入活动文件；
- 所有数据结构必须包含 `schemaVersion`，支持升级迁移。

## 17. 非功能需求

### NFR-001 离线能力

- 核心闭环无需手机、网络、Garmin Connect 或账号。
- 手机断开不影响抛竿识别、倒计时和震动。

### NFR-002 性能

- 抛竿识别结果延迟 P95 ≤ 2 秒；
- UI 每秒刷新时无明显卡顿；
- 传感器回调不得执行阻塞 I/O；
- 仅保留必要短窗口，不无限积累数组；
- 连续运行 6 小时不得出现内存增长导致的崩溃。

### NFR-003 电量

开发验收目标：在首发目标设备上，6 小时会话耗电中位数不超过 30 个百分点，且不得导致设备过热。

这是暂定工程目标，最终应按机型分别公布实测结果。优化优先级：

1. 减少 UI 重绘；
2. 候选门控后再计算复杂特征；
3. 正式版关闭原始 SensorLogging；
4. 陀螺仪按设备和模型收益决定是否开启；
5. 不持续点亮背光。

### NFR-004 可靠性

- 所有传感器注册、活动创建、存储和震动调用必须捕获异常；
- `Attention`、提示音、陀螺仪等必须做运行时能力检查；
- 退出和崩溃恢复后不能重复增加抛竿次数；
- 结束会话后必须释放传感器监听和 Timer；
- 低电量时提示用户，但不未经确认自动结束会话。

### NFR-005 可用性

- 戴手套或手湿时仍可通过物理键完成核心操作；
- 关键状态不依赖颜色；
- 震动模式可凭触感区分；
- 主界面无需精细触控；
- 老用户启动路径不超过 3 次按键。

### NFR-006 隐私

- 正式版默认仅在设备本地处理传感器数据；
- 不采集位置、联系人或账号信息；
- Beta 诊断日志必须由用户明确开启；
- 发布页和隐私政策必须说明传感器用途、保存范围和删除方式；
- 若未来上传数据，必须另行获得用户同意并提供删除机制。

## 18. 埋点与产品指标

MVP 无云端，因此以下指标先以本地摘要和 Beta 人工回收获得：

### 18.1 核心产品指标

- 完成至少 30 分钟会话的比例；
- 单次会话时长；
- 单次会话抛竿次数；
- 自动识别率：`autoCastCount / castCount`；
- 手动补记率：`manualCastCount / castCount`；
- 撤销率：`undoneCastCount / autoCastCount`；
- 用户保持运行 2 小时、4 小时、6 小时的比例；
- 7 日内第二次使用比例；
- 用户自评“比手动计时更省事”的比例。

### 18.2 MVP 成功标准

在不少于 30 名目标用户、每人不少于 3 次真实垂钓会话的 Beta 中：

- ≥ 80% 的有效会话无需手动补记超过 10% 的抛竿；
- ≥ 80% 的有效会话误识别撤销不超过 1 次/2 小时；
- ≥ 70% 的用户完成至少 3 次会话；
- ≥ 60% 的用户明确表示愿意继续使用或付费；
- 不出现因到点提醒完全失效而未被发现的系统性问题。

## 19. 验收标准

### AC-001 首次抛竿

**Given** 用户已选择 3 分钟并开始会话  
**When** 应用在支持场景下识别到第一次抛竿  
**Then** 抛竿次数变为 1，短震确认，并显示从 03:00 开始的倒计时。

### AC-002 中途重新抛竿

**Given** 当前剩余 01:20  
**When** 识别到新的抛竿  
**Then** 抛竿次数加 1，倒计时重置为 03:00，旧终点失效。

### AC-003 到点提醒

**Given** 当前存在有效 deadline  
**When** 到达 deadline  
**Then** 在 ±1 秒内进入 `DUE`，执行三段震动，并显示超时时间。

### AC-004 到点后重新抛竿

**Given** 当前处于 `DUE`  
**When** 识别到新抛竿  
**Then** 停止后续超时提醒，抛竿次数加 1，开始新一轮完整倒计时。

### AC-005 漏识别补记

**Given** 用户已经抛竿但自动识别未触发  
**When** 用户按确认键  
**Then** 生成 `manual_cast`，计数加 1，并开始或重置倒计时。

### AC-006 误识别撤销

**Given** 10 秒内刚产生一次抛竿事件  
**When** 用户按返回键撤销  
**Then** 抛竿次数、倒计时终点和状态恢复到识别前。

### AC-007 暂停

**Given** 倒计时剩余 90 秒  
**When** 用户暂停 5 分钟后选择“继续剩余时间”  
**Then** 恢复后仍从约 90 秒继续，暂停期间不提醒。

### AC-008 无手机

**Given** 手机断开连接  
**When** 用户完成一次完整抛竿—计时—提醒—再次抛竿循环  
**Then** 所有核心功能正常工作。

### AC-009 异常恢复

**Given** 当前会话未正常结束且保存了 deadline  
**When** 用户重新打开应用并恢复  
**Then** 应用按绝对时间恢复剩余时间或显示已经超时，不从完整间隔重新开始。

### AC-010 不支持设备

**Given** 设备无法提供所需高频加速度或震动  
**When** 用户尝试开始会话  
**Then** 应用明确提示限制，不进入假运行状态。

## 20. 测试与数据采集计划

### 20.1 Phase 0：传感器可行性

建议样本：

- 20 名真实钓鱼用户；
- 每人至少 50 次抛竿；
- 合计不少于 1,000 次正样本；
- 每人至少 20 分钟非抛竿负样本；
- 覆盖左/右手、单/双手、纺车轮/鼓轮、过头抛/侧抛；
- 至少覆盖 fēnix、Forerunner、Venu 三类首发设备。

评测规则：

- 训练、阈值选择和最终测试必须按用户完全隔离；最终测试用户不得参与参数调优；
- Precision、Recall、每小时误触发和延迟必须同时按设备、佩戴/持竿手和抛竿方式分组报告，不得只报告总体值；
- 每类负样本的时长、动作次数和误触发次数必须原样保留，使每小时误触发率可复算；
- 视频与手表日志必须在每次会话开始和结束建立同步标记，视频真值优先于按键标记；
- 首发只评估“手表在主要发力手”的组别。非发力手数据用于判断是否扩展支持范围，不得混入首发指标。

标签方式：

- 视频时间戳作为主要真值；
- 手表按键标记作为辅助；
- 每次误识别记录实际动作类别；
- 数据按“用户”划分训练集和测试集，禁止同一用户的高度相似动作同时进入训练与测试造成虚高。

### 20.2 负样本专项测试

至少执行第 13.5 节列出的动作，每类不少于 100 次或累计不少于 30 分钟。

### 20.3 实机稳定性

每个首发机型至少完成：

- 6 小时连续运行；
- 100 次抛竿事件；
- 20 次手动重置；
- 20 次撤销；
- 10 次暂停/恢复；
- 5 次任务切换和恢复；
- 低电量场景；
- 手机连接/断开切换；
- FIT 保存成功和失败处理。
- 屏幕变暗但应用仍 active 时的采样与提醒；
- inactive 后的采样、到点状态保存，以及恢复 active 后的超时对账；
- 活动 Session 冲突时不修改既有活动的安全失败路径；
- SensorLogging 的实际采样率、FIT 文件大小和续航影响。

### 20.4 模拟器测试

模拟器适合验证：

- 状态机；
- UI 和设备布局；
- 倒计时；
- 存储迁移；
- 按键映射；
- FIT 会话生命周期。

模拟器不能代替真实抛竿识别和实际电量测试。可使用记录的 FIT/传感器文件回放做回归测试。

### 20.5 自动化测试清单

以下测试属于 MVP Definition of Done，不应推迟到公开发布之后：

| 测试对象 | 必测内容 |
|---|---|
| 状态机 | 每个合法转换、非法事件忽略、暂停恢复、结束清理 |
| CountdownEngine | 首次开始、提前重置、到点、超时、时钟跳变、恢复后重算 |
| 撤销 | `ARMED`、`COUNTING`、`DUE` 三种来源状态的完整回滚 |
| ReminderManager | 三种震动模式、重复计划、取消、重复调用去重、能力缺失 |
| 持久化 | 正常保存、字段缺失、旧 schema 迁移、Storage 满、损坏数据降级 |
| ActivityRecording | 创建失败、已有活动、start/stop/save 返回 false、异常退出 |
| Sensor 生命周期 | 注册失败、回调空数据、注销、暂停恢复、inactive/active 切换 |
| CastDetector | 正样本、每类负样本、左右手镜像、设备回放、同一动作去重 |
| 手动模式 | 传感器不可用时的明确提示、手动抛竿、到点提醒和摘要 |
| UI | 圆屏、方屏、长文本、按钮与触屏输入 |

传感器算法每次调整阈值、滤波器或特征权重时，都必须运行固定回放数据集，并与上一个发布模型比较 Precision、Recall、每小时误触发和设备分组结果。不得只看总体准确率。

## 21. 开发工作包与复杂度

| 工作包 | 主要产出 | 复杂度 | 关键依赖 |
|---|---|---:|---|
| WP0 SDK/设备 Spike | 有效采样率、活动 Session 冲突、active/inactive、震动、屏幕休眠与续航验证 | 中 | 至少 2 类真实手表 |
| WP1 数据采集工具 | 传感器日志、日志完整性校验、按键/视频同步标签、设备信息 | 中 | SensorLogging、FIT 导出、现场用户 |
| WP2 数据采集与标注 | ≥1,000 次抛竿及负样本 | 高 | 水边测试、视频标注 |
| WP3 抛竿识别算法 | 特征、模型、回放评测、设备参数 | 高 | WP2 数据质量 |
| WP4 会话与倒计时 | 状态机、提醒、暂停、恢复、撤销 | 中 | 无 |
| WP5 手表 UI | 设置、活动页、到点页、摘要、输入适配 | 中 | 设备资源 |
| WP6 FIT 与存储 | SPORT_FISHING、摘要、异常恢复 | 中 | Fit 权限 |
| WP7 实地 Beta | 30 用户 × 3 会话、问题归因 | 高 | 招募和设备覆盖 |
| WP8 商店发布 | 多语言、隐私、图标、兼容列表 | 低～中 | Beta 达标 |

粗略评估：如果已有一名熟悉 Connect IQ/Monkey C 的工程师，并能稳定获得真实钓鱼数据，从技术 Spike 到可用 Beta 通常需要约 12～20 个自然周。其中手表端工程约 5～7 周，真实数据采集、标注、回放评测、机型回归和 Beta 验证是主路径。若 Phase 0 前已具备合格的带标签数据，才可能压缩至 8～12 周。

## 22. 主要风险与应对

| 风险 | 严重度 | 影响 | 应对 |
|---|---|---|---|
| 手表戴在非发力手，信号不足 | 极高 | 核心功能不可用 | Phase 0 分组测试；首版限制支持范围 |
| 收线/扬竿等误识别 | 极高 | 错误重置导致提醒延后 | 高 Precision 优先；撤销；状态与不应期 |
| 漏识别 | 高 | 用户仍需手动操作 | 单键补记；校准；显示自动识别质量 |
| 设备碎片化 | 高 | 某些型号传感器/内存表现不同 | 白名单发布；按设备配置和回归测试 |
| 长时间高频采样耗电 | 高 | 6 小时垂钓无法完成 | 25Hz 起步；候选门控；减少重绘；关闭正式版日志 |
| 实际采样率或诊断日志分辨率不足 | 高 | 模型不可训练、回放与线上表现不一致 | WP0 实测回调和 FIT 日志；按机型白名单；必要时设计经同意的候选窗口导出 |
| 用户切换 App 后无法震动 | 高 | 到点提醒失效 | 前台提醒边界写入首次说明和商店文案；inactive 保存状态；恢复后对账显示 |
| 与原生钓鱼活动冲突 | 高 | 误接管或破坏既有活动 | WP0 验证 Session 所有权与冲突检测；无法可靠识别时安全拒绝启动 |
| 飞钓/路亚动作模式差异 | 中 | 泛化差、差评 | 商店描述明确支持场景；后续独立模型 |
| 到点震动被系统/用户设置弱化 | 中 | 用户未感知 | 多段模式；实机检查；可选提示音 |
| 原始传感器数据带来隐私顾虑 | 中 | Beta 招募和商店审核 | 默认本地；诊断日志显式同意；可删除 |

## 23. 发布策略

### 阶段 A：内部技术验证

- 只做数据采集、传感器回放和最小倒计时；
- 5～10 名用户；
- 不提交公开商店；
- 目标是通过 Phase 0 门槛。

### 阶段 B：封闭 Beta

- 30 名目标用户；
- 限定设备白名单；
- 提供手动补记、撤销和诊断日志；
- 每次会话后收集误识别和漏识别反馈。

### 阶段 C：Connect IQ Store 首发

- 只声明已经完成实机验证的设备；
- 商店文案明确要求的佩戴方式和支持钓法；
- 核心功能完全离线；
- 首版不承诺所有抛竿动作均可识别；
- 发布简体中文和英文。

## 24. 开发评审必须回答的问题

### 产品决策

1. 首发是否只支持“手表戴在主要发力手”？
2. 首发是否限定纺车轮、鼓轮和过头/侧抛？
3. 快捷预设使用鱼种名称，还是只使用时间节奏名称？
4. 到点后重复震动次数是否采用 0、60、120 秒方案？
5. 是否允许用户关闭抛竿确认短震？
6. 是否将每次会话保存为 Garmin Fishing 活动？本 PRD 建议“是”。

### 技术决策

1. 各目标设备在屏幕变暗但应用仍 active 时，实际有效采样率是否稳定达到 25Hz？若否，最低可用采样率是多少？
2. 记录活动时，任务切换到 inactive 后传感器数据是否按文档持续？
3. inactive 状态无法使用 Attention 时，是否存在合规替代机制？预期答案可能是“没有”。
4. 加速度计单独能否达到门槛，还是必须缩小到支持陀螺仪的设备？
5. 不同设备的轴方向、量程和采样时间戳是否需要独立归一化？
6. 是否使用 FIT developer field 写入抛竿总数和自动识别率？
8. SensorLogging 的实际采样率、回放可用性、续航和 FIT 文件大小如何？是否足以支撑模型训练？
9. 如何可靠检测既有 ActivityRecording Session 的冲突及所有权，且不修改原生活动？
10. 应用异常终止后，ActivityRecording Session 的恢复/保存行为如何？
11. Connect IQ Store 对 Fishing 活动、自定义传感器日志和隐私说明有哪些审核要求？

## 25. 最终立项判断

本产品的倒计时、状态机、震动提醒和 Garmin 活动记录均属于低到中等技术风险。真正决定项目是否成立的是抛竿识别，而不是 UI 或计时器。

因此推荐的立项顺序为：

1. 只立项 Phase 0：先验证采样率、活动冲突、前后台能力和数据采集链路；
2. 用真实用户验证佩戴手与抛竿动作是否可分；
3. 达到 Precision/Recall 门槛后，再完成产品闭环；
4. 如果只能在“手表戴在主要持竿手”时达到要求，则把它写成明确产品条件；
5. 如果即使在受控场景下误触发仍超过 0.5 次/小时，应停止自动识别路线，转为“一键抛竿 + 自动循环提醒”，而不是勉强上线。

## 26. Definition of Done

MVP 只有同时满足以下条件才算完成：

- [ ] Phase 0 的 Precision、Recall、误触发和延迟门槛全部通过；
- [ ] 明确发布支持的佩戴手、钓法和设备白名单；
- [ ] 核心闭环在无手机情况下完整通过；
- [ ] 目标机型完成 6 小时稳定性和电量测试；
- [ ] 已验证活动 Session 冲突的检测与安全失败路径；本应用不会复用、接管或修改既有活动；
- [ ] 每个首发机型的实际采样率与 SensorLogging/回放数据完整性已实测并达到模型要求；
- [ ] 传感器失败时明确阻止自动模式或进入显式手动模式；
- [ ] 到点、重复提醒、取消和下一次抛竿重置均通过实机验证；
- [ ] 暂停、撤销、异常退出和恢复均有自动化测试；
- [ ] 固定传感器回放集进入持续回归流程；
- [ ] fēnix、Forerunner、Venu 中的首发型号分别通过 UI 和输入测试；
- [ ] 正式版默认不记录连续原始传感器数据；
- [ ] 中英文文案、隐私政策、商店描述和已知限制完成；
- [ ] 30 人封闭 Beta 达到第 18.2 节成功标准；
- [ ] 所有未解决的开发评审问题均有负责人、截止点或明确的发布阻断标记。

## 27. 技术参考

- [Garmin Connect IQ SDK 概览（当前公开版本 9.2.0）](https://developer.garmin.com/connect-iq/overview/)
- [Garmin App Types](https://developer.garmin.com/connect-iq/connect-iq-basics/app-types/)
- [Garmin 高频传感器 API：Toybox.Sensor](https://developer.garmin.com/connect-iq/api-docs/Toybox/Sensor.html)
- [Garmin Sensors 指南](https://developer.garmin.com/connect-iq/core-topics/sensors/)
- [Garmin Attention：震动、提示音与背光](https://developer.garmin.com/connect-iq/api-docs/Toybox/Attention.html)
- [Garmin Getting the User's Attention](https://developer.garmin.com/connect-iq/core-topics/getting-the-users-attention/)
- [Garmin ActivityRecording](https://developer.garmin.com/connect-iq/api-docs/Toybox/ActivityRecording.html)
- [Garmin 应用生命周期与 active/inactive 资源限制](https://developer.garmin.com/connect-iq/core-topics/application-and-system-modules/)
- [Garmin Timer.Timer](https://developer.garmin.com/connect-iq/api-docs/Toybox/Timer/Timer.html)
- [Garmin Application.Storage](https://developer.garmin.com/connect-iq/api-docs/Toybox/Application/Storage.html)
- 本机 Connect IQ SDK 9.1.0 官方示例：`samples/PitchCounter`、`samples/AccelMag`、`samples/RecordSample`
